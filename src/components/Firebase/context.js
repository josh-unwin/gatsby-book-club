import React from "react";

// This creates a React Context for Firebase (a React context is data that can be accessed from any component)
const FirebaseContext = React.createContext(null);

export default FirebaseContext;
